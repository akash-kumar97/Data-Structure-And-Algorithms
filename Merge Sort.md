Merge sort is a type of sorting. Its time complexity is O(nlogn).

Algorithm - 

In this we will divide the array till all elements are divided separately using recursive function.

Then we will sort them.
